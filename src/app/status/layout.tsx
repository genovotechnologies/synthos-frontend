export const metadata = {
  title: 'System Status | Synthos',
  description: 'Check the current status of Synthos services.',
};

export default function StatusLayout({ children }: { children: React.ReactNode }) {
  return children;
}
