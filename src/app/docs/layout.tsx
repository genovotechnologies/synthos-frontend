export const metadata = {
  title: 'API Documentation | Synthos',
  description: 'Synthos API reference documentation. Integrate AI training data validation into your ML pipeline.',
};

export default function DocsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
